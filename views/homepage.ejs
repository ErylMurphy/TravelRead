<!doctype html>
<html>
  <head>
    <% include ./partials/header %>
  </head>
  <body>
    <div class="page-container">

      <h2 class="page-title">All States</h2>
      <% states.forEach(state => { %>
        <p><a href="/states/<%= state.id %>"><img src="<%= state.state_img %>"></a></p>
      <% }) %>
    </div>
  </body>
</html>
