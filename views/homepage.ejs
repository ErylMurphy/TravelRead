<!doctype html>
<html>
  <head>
    <% include ./partials/header %>
  </head>
  <body>
    <div class="page-container">
      <div class = "allpostcards">
      <% states.forEach(state => { %>
        <p><a href="/states/<%= state.id %>"><img class = "postcard" src="<%= state.state_img %>"></a></p>
      <% }) %>
      </div>
    </div>
  </body>
</html>
